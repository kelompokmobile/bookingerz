<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jemberonlineservice.bookingerz">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.RegisterActivity"
            android:label="@string/register_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.AddAcaraActivity"
            android:label="@string/addacara_name"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.SettingActivity"
            android:label="@string/setting_name"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.DetailAcaraActivity"
            android:label="@string/detailacara_name"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.ProfileActivity"
            android:label="@string/profile_name"
            android:launchMode="singleTop" />
    </application>

</manifest>